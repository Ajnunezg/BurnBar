# Legacy Operating Layer Archive

This folder keeps the pre-split operating-layer implementation as a historical reference only.

- It is not part of the app target.
- The live implementation now lives under `AgentLens/Services/BurnBarOperating/`.
- Keep new product work out of this archive; use git history if you need older context beyond this snapshot.
